'use strict';

const sqs = require('../../lib/service/sqs');
const queue = require('../../lib/service/queue');

describe('queue-service', () => {
  describe('sendMessages', () => {
    it('should send messages to the right sqs queue', () => {
      
    });
  });
});